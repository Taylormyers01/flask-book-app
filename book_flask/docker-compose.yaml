version: "3.9"

services:
  backend:
    build: .
    container_name: book_flask
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONPATH=/app/src
    restart: always
